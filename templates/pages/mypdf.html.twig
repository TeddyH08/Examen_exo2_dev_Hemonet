<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    {{ encore_entry_link_tags('app') }}
</head>
<body>
    <div class="title_pdf">
        <h1>Données du projet : {{ project.name }}</h1>
    </div>

    <h3>Image : {{ project.image }}</h3>
    <h3>Créateur : {{ project.admin.email }}</h3>
    <h3>Date de création : {{ project.createdat|date("d/m/Y") }}</h3>
    <h3>Lien du site du projet : {{ project.link }}</h3>
    <h3>Lien du github du projet : {{ project.github }}</h3>
</body>
</html>